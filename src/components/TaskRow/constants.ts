import type { CSSProperties } from 'react';

export const TASK_ROW_BUTTON_STYLE: CSSProperties = {
    all: 'unset',
    display: 'flex',
    gap: 12,
    alignItems: 'center',
    flex: 1,
    minWidth: 0,
    cursor: 'pointer'
};
