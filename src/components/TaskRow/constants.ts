import type { CSSProperties } from 'react';

export const TASK_ROW_BUTTON_STYLE: CSSProperties = {
    all: 'unset',
    display: 'flex',
    gap: '0.75rem',
    alignItems: 'center',
    flex: 1,
    minWidth: 0,
    cursor: 'pointer'
};
