export const MAX_TEXT_LENGTH = 200;
export const MAX_PHOTO_BYTES = 5 * 1024 * 1024; // keep in sync with next.config.ts bodySizeLimit '5mb'
export const MAX_DAYS = 3650;
export const MAX_MONTHS = 600;
