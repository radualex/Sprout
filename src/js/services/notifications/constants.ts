/** Minimum interval for periodic background sync (12 hours). */
export const PERIODIC_SYNC_MIN_INTERVAL_MS = 12 * 60 * 60 * 1000;

/** How often to re-check for due care while the app is open (1 hour). */
export const CARE_CHECK_INTERVAL_MS = 60 * 60 * 1000;
